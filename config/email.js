export const MAIL_SERVICE_CONFIG = {
  // Endpoint you own that accepts POSTed JSON: { email, subject, message, pdfBase64, filename, meta }
  // Leave blank to disable emailing until a Mailchimp/transactional proxy is configured.
  endpoint: "",
  source: "value-calculator"
};
